# 🐍 Avaliação Prática: Fundamentos de Python para IA

**Disciplina:** Inteligência Artificial  
**Duração:** 90 minutos  
**Pontuação Total:** 100 pontos  

Bem-vindo(a) à sua avaliação prática! Este repositório contém o código inicial que você deverá completar. Leia atentamente as instruções abaixo antes de começar.

---

## 🎯 Objetivo
Você deve preencher as funções vazias no arquivo da sua prova (`prova_a.py` ou `prova_b.py`, conforme orientação do professor). Cada função possui comentários detalhados explicando exatamente o que é esperado como entrada e saída.

⚠️ **Atenção:** Você é responsável por testar o seu código localmente antes de enviar.

---

## 🚀 Como iniciar o teste no PyCharm

Siga os passos abaixo para clonar este repositório diretamente no seu PyCharm:

1. Abra o PyCharm.
2. Na tela inicial, clique em **"Get from VCS"** (ou vá no menu superior em `Git` > `Clone...`).
3. Copie a URL deste repositório aqui no GitHub (botão verde `<> Code` > Copiar URL).
4. Cole a URL no campo **"URL"** no PyCharm.
5. Escolha a pasta na sua máquina onde deseja salvar o projeto no campo **"Directory"**.
6. Clique no botão **"Clone"**.
7. O PyCharm vai carregar o projeto. Se ele perguntar se você confia no projeto, clique em **"Trust Project"**.

---

## 💻 Como realizar a prova

1. No painel lateral esquerdo do PyCharm (Project), abra o arquivo da sua prova (`prova_a.py` ou `prova_b.py`).
2. Leia os comentários de cada Missão/Questão.
3. Substitua o comando `pass` pela sua lógica de programação.
4. **Para testar o seu código:** No final do arquivo, dentro do bloco `if __name__ == "__main__":`, você pode criar chamadas `print()` com dados fictícios para rodar o arquivo e ver se a sua função está retornando o valor correto.
5. Para rodar o código no PyCharm, clique com o botão direito em qualquer lugar do arquivo e selecione **"Run 'prova_...'"** (ou use o atalho `Shift + F10`).

---

## 📤 Como enviar a prova e receber a nota

Sua prova será corrigida **automaticamente** assim que você enviar o código de volta para o GitHub. 

1. No PyCharm, abra a aba **"Commit"** no painel lateral esquerdo (ou vá no menu superior `Git` > `Commit`).
2. Marque a caixinha ao lado do arquivo que você modificou (`prova_a.py` ou `prova_b.py`).
3. No campo de mensagem (Commit Message), digite algo como: `Finalizando a prova`.
4. Clique no botão azul **"Commit and Push..."** na parte inferior.
5. Confirme clicando em **"Push"** na janela que abrir.

Boa sorte! 🍀
